---

# Disable raw commands to avoid sudo issues
bootstrap_raw: False

# Don't set domain on Travis
boostrap_domain: ''

# Try bootstrapping a different IP address to avoid idempotency loop
boostrap_ipv4: '127.0.1.2'

