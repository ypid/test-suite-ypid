---

- hosts: all
  sudo: True

  roles:

    - role: debops.secret

  tasks:
    - debug: msg="Check if {{ inventory_dir }} variable is defined"


- include: '/home/travis/build/debops/debops-playbooks/playbooks/site.yml'

