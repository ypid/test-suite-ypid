---

- hosts: 'placeholder_fqdn'
  become: True

  roles:
      # Dovecot requires configured SSL certificates
    - role: 'debops.pki'

      # Install and configure Dovecot using cloned role
    - role: 'ansible-dovecot'

