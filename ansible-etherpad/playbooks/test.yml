---

- hosts: 'placeholder_fqdn'
  sudo: True

  roles:
      # Nginx requires configured SSL certificates
    - role: 'debops.pki'

      # Install and configure Etherpad using cloned role
    - role: 'ansible-etherpad'

