#!/bin/bash

# test: Test debops.debug
# Copyright (C) 2014 Nick Janetakis <nick.janetakis@gmail.com>
# Part of the DebOps project - http://debops.org/


. "${ROLESPEC_LIB}/main"


install_ansible


assert_playbook_runs


# Template file generated by this role will always change, because some of the
# fields are writing current date. This means that the playbook idempotency will
# never be achieved, so we should not test for this.
#assert_playbook_idempotent

assert_path "/tmp/ansible_variables"
